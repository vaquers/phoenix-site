[build]
builder = "nixpacks"

[deploy]
startCommand = "cd phoenix_bot && bash start.sh"
healthcheckPath = "/api/about"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
